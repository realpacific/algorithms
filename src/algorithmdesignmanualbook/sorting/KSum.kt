package algorithmdesignmanualbook.sorting

import algorithmdesignmanualbook.print

/**
 * Given a set S of n integers and an integer T,
 * give an O(nk−1 log n) algorithm to test whether k of the integers in S add up to T.
 *
 * References https://www.geeksforgeeks.org/find-the-k-th-permutation-sequence-of-first-n-natural-numbers/
 */
private fun kSum(array: Array<Int>, k: Int) {

}

fun main() {
    val input = arrayOf(1, 2, 3, 0, -1, -4, -2, 4, 5, 6, 7, 8).sortedArray().print()
    TODO()
}

